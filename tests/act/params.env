# for best reproducibility, use the full image
IMAGE=catthehacker/ubuntu:full-24.04
# on some systems, the base image can work
#IMAGE=catthehacker/ubuntu:act-24.04
NXF_VER=25.10.0
DEFAULT_PROFILE=docker
